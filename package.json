{
  "name": "swiftdroom",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm ci --prefix dashboard",
    "build": "npm run build --prefix dashboard",
    "start": "node scripts/verify-env.mjs && npm run start --prefix dashboard"
  }
}
