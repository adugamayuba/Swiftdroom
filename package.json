{
  "name": "swiftdroom",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm ci --prefix dashboard --include=dev",
    "build": "npm ci --prefix dashboard --include=dev && npm run build --prefix dashboard",
    "start": "node scripts/verify-env.mjs && node scripts/start-production.mjs"
  }
}
